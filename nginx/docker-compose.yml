version: '3.8'

services:
  nginx:
    image: nginx:alpine
    container_name: tickatch-nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    networks:
      - tickatch-network
    restart: unless-stopped

networks:
  tickatch-network:
    external: true